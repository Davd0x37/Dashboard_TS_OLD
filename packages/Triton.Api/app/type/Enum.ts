export enum DataType {
  ACCOUNT = "account",
  DROPLETS = "droplets"
}

export enum ErrorType {
  NotFound = 1,
  RequestFailed = 2,
  TokensNotExists = 3,
  BadStateKey = 4,
  AccessTokenRequestFailed = 5
}