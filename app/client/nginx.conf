events {}
http {
    server {
        listen 3030;
        server_name haven;
        root ./dist;
        index index.html;
        # Force all paths to load either itself (js files) or go through index.html.
        location / {
            root ./dist;
            try_files $uri /index.html;
        }
    }
}